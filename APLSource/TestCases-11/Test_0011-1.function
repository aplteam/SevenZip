 r←Test_0011(stopFlag batchFlag);zipFilename;list;filename;myZipper;rc;more;found;⎕TRAP
 ⍝ Provides a filename twice, once in the root and also in the "foo" folder, with relative paths.
 r←∆Failed
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')

 zipFilename←∆OutputPath,'zip_011'
 list←''
 list,←⊂∆InputPath,'ExistsTwice.txt'
 list,←⊂∆InputPath,'foo/ExistsTwice.txt'

⍝ Delete any left overs
 #.FilesAndDirs.DeleteFile zipFilename

⍝ The test
 myZipper←⎕NEW ##.SevenZip(,⊂zipFilename)
 rc←myZipper.Add list
 →GoToTidyUp 0∨.≠3⊃rc
 →GoToTidyUp(#.FilesAndDirs.NormalizePath¨list)≢2⊃myZipper.List 0
 ⎕EX'myZipper'
 r←∆OK

∆TidyUp:
 #.FilesAndDirs.DeleteFile zipFilename
